{{#each posts}}
    <div class="card">
        <img src="images/homestead-automation-logo.svg" alt="">
        <div class="container">
            <h3>{{postTitle}}</h3>
            <p>{{postBody}}</p>
        </div>
    </div>
{{else}}
    <h2>No Posts</h2>
{{/each}}